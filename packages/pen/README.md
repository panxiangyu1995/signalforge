# @signal-forge/pen

Pencil.dev `.pen` document parser and SceneGraph adapter.

This package owns the `.pen` document model and import policy. It depends on `@signal-forge/scene-graph` for the shared editable design model and does not depend on `@signal-forge/core`.
